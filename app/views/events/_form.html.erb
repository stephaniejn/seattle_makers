    <%= bootstrap_form_for @event do |f| %>
    <div class="form-group">
        <%= f.text_field :title, placeholder: 'Enter Event Title' %>
    </div>
    <div class="form-group">
        <%= f.text_area :desc, placeholder: 'Enter Event Description' %>
    </div>
    <div>
        <%= f.text_field :capacity, placeholder: 'Event Capacity' %>
    </div>
    <div>
        <%= f.hidden_field :donation, placeholder: 'Accept Donation?' %>
       <!--  <div class="btn-group" data-toggle="buttons">
          <label class="btn btn-default">
            <input type="radio" name="options" id="option1" autocomplete="off" checked> $5.00
          </label>
          <label class="btn btn-default">
            <input type="radio" name="options" id="option2" autocomplete="off"> $10.00
          </label>
          <label class="btn btn-default">
            <input type="radio" name="options" id="option3" autocomplete="off"> $15.00
          </label>
        </div> -->
    </div>
    <div class="form-group">
        <%# @categories.each do |category| %>

        <%= f.select :category_id, options_from_collection_for_select(@categories, "id", "name"), {:prompt => "Select A Category"}, required: true %>
    </div>
        <%= f.submit 'Submit', class: 'btn btn-primary' %>
    <% end %>
</div>
