
<h1 style="text-align:center"><%= @event.title%> <%if @current_user%> <%= link_to 'Edit Event', edit_event_path, :class=>'btn' %><%end%></h1>
<!-- need to set to not just current user but if current user is host, if @current_user = @event.user_id doesn't work-->
<br> <br>

<div class='row'>
<div class='col-xs-offset-4 col-md-offset-1'>

    <div class='col-xs-5 col-md-5'><img src = "<%=asset_path "placeholder.png"%>"></div>

    <div style="text-align:center" class='col-xs-5 col-md-5'><p> <strong> Event Description: </strong> <%= @event.desc%> </p>
    <p> <strong> Host: </strong><%= @event.user.name%></p>
    <p> <strong> Category: </strong><%= @event.category.name%></p>
    <p> <strong> Capacity: </strong> <%= @event.capacity%> </p>
    <p> <strong> Donation Suggested? </strong> <%= @event.donation%> </p>
    <br>
    <br>
    <%= link_to 'See other events by ' + @event.user.name, profile_path(@event.user.id) %>
    </div>
  </div>
</div>

